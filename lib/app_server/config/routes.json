{
  "GET" :
    {
      "resource" : "/game.haml",
      "route" : "GameController.new.send(:run)"
    },
  "POST" :
    {
      "resource" : "/board.haml",
      "route" : "BoardController.new.send(:run, body)"
    }
}
